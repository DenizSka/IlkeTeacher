<!--
<h1> Accepting... </h1>

<ul>
<% setTimeout(function () { %>
<% supplies = [1, 2, 3, 4]; %>
<% for(let i=0; i<supplies.length; i++) {%>
   <li><%= supplies[i] %></li>
<% } %>
<% }, 1000) %>
</ul>

(function($) {
  $(function() {
    $('#test').click(function(e) {
      if (e.originalEvent) {
        alert('You clicked me!');
      } else {
        alert('Something else triggered me!');
      }
    });

    setTimeout(function() {
      $('#test').trigger('click');
    }, 5000);
  });
})(jQuery);
{{ method_field('PUT') }} -->

<% include ../partials/header %>

<div class='proje'>
<form method='POST' action="/admin/pending/<%= pendinguser.id %>?_method=PUT">
  <h5 class="publiname-single">User #: <%= pendinguser.id %></h5>
  <p class="publiname-single">Email: <%= pendinguser.email %></p>
  <p class="publiname-single">Full Name: <%= pendinguser.fullname %></p>

    <button type="Submit" value="Accept" class="btn btn-secondary">
    Accept
    </button>
</form>
    <td><form method='POST' action='/admin/pending/<%= pendinguser.id %>?_method=DELETE'>
      <button type="Submit" value="Decline" class="btn btn-secondary">
      Decline
      </button>
    </tr>

</div>


<% include ../partials/footer %>
